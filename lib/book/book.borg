(project borge-lib-book
 (section book
  (doc "Codebase book printer. lib/book/")
  (status planned)
  (subsection book-print-impl
   (doc "Linearize source files to a paginated PDF (inline-aware order: the root's (inline ...) tree, then unreferenced files lexicographically; register is a render flag: --mode workshop|reader, default workshop)")
   (status implemented)
   (* agent note (|
     P1.6 (2026-08-30): example fences. Book_structure grew the
     shared fence parser (fence_split): ```nopales/```ocaml doc
     fences + optional `expect =>` trailer -> {lang, src, expected?}
     records, collected per chapter in document order, mode-
     independent (content, not scaffolding). ONE parser, two
     consumers (decision 3): print renders segments (text ->
     paragraphs, example -> lstlisting without line numbers + the
     [expect => ...] captured-output annotation), export emits the
     records in the chapter's examples field. Doc node bodies stay
     verbatim (fences + trailer ride along); unparsed fences (other
     languages, unterminated) stay raw text everywhere. The expect
     trailer IS the v1 captured-output record — print never executes
     code (decision 4: no live OCaml evaluator in v1).
   |) *)
   (* agent note (|
     verified at this commit: build-pass, smoke-pass (borge book print
     . opens with borge.borg as the introduction, then the other 8
     top-level .borg design docs, then .ml implementations; line
     numbers on every page; tree_checksum deterministic across runs).
     Uses lstinputlisting with sanitized temp files to dodge the UTF-8
     and \end{lstlisting}-in-source problems.
   |) *)
   (* agent note (|
     v2 walk (2026-08-29, habitat-book workstream): the four-tier +
     dependency-rank ordering was REPLACED by the inline-aware walk —
     chapter order = Project.build_tree flatten of the root's
     (inline ...) tree, unreferenced files appended lexicographically.
     Why: pricklypear's book printed chapters in readdir order at
     p788+ with a clone-dependent TOC; the spec-as-book rule (one walk
     shared by print/export/habitat-loader) needs the inline tree as
     the single ordering authority. Verified: pricklypear manifest
     order matches borge inline order (philosophy -> ... ->
     habitat-book, code appendix after); alcotest book_walk suite
     covers order/appendix/missing-target/duplicate-inline.
   |) *)
   (* agent note (|
     P1.2 (2026-08-29): determinism + --root. PDFs are now
     byte-identical across runs (pdflatex runs with cwd in the work
     dir, listings cited relatively, SOURCE_DATE_EPOCH +
     FORCE_SOURCE_DATE pin creation date and /ID; manifest
     rendered_at stays the only wall-clock field). --root FILE
     prints exactly the inline subtree rooted at FILE (no appendix;
     hard error on missing file or unbuildable tree, unlike the
     default walk's degrade-to-lone-chapter). Verified: alcotest
     book_walk now also covers subtree selection, --root errors,
     tex byte-determinism, and a full two-run PDF byte-compare.
   |) *)
   (* agent note (|
     P1.3 (2026-08-29): type coverage. .pp (Nopales product layer)
     and .sql (migrations) are code chapters now — is_source accepts
     them, listings are cited under their own extension (%04d.pp /
     %04d.sql), and they land in the lexicographic appendix since
     the inline tree never names them. backups/ joined skip_dirs:
     pp-sync snapshot vault dumps are gitignored copies of branch
     state (pricklypear alone holds 368 .pp dumps there), not source.
   |) *)
   (* agent note (|
     P3.1 (2026-08-31): course composition roots. course/ joined
     skip_dirs and the default walk filters course roots from
     Project.find_roots (shared default_roots, also used by
     book_export's walk_root so the manifest names the canonical
     book). Why: a course/*.borg root is an implicit second root
     (it inlines chapters) sorting BEFORE pricklypear.borg — the
     canonical TOC would silently reorganize on course authoring.
     Courses are compositions (habitat-book decision 5): --root
     course/x.borg still selects the exact subtree, with ../
     inline targets canonicalized in canon_path so chapters dedup
     against root-written spellings. Verified: alcotest
     test_course_composition; pricklypear canonical export stays
     34ch/root pricklypear.borg with course/cs-interpreters.borg
     present; --root export = 4 chapters in course order; --root
     print = 48pp PDF, course TOC.
   |) *)
   (* agent note (|
     P1.4 (2026-08-29): reader/workshop modes. Register is a render
     mode, not a document fork (habitat-book decision 1).
     Workshop (default) renders status labels, verify blocks as one
     compact scaffold line, agent-note blockquotes, and untracked
     notes. Reader strips all four; prose, headings, inline notes,
     and the drift-hole fallback listing stay in BOTH registers.
     One deliberate delta vs pre-v2 output: (verify ...) and
     (untracked ...) now render in workshop — the spec mandates it
     (statuses/verify/agent-notes visible in the workshop
     register); they were silently skipped before. Verified:
     alcotest mode case (workshop keeps / reader strips; prose,
     headings, inline kept in both); real reader-vs-workshop PDF
     text diff on pricklypear philosophy (--root) = exactly the
     status x2 + verify x1 scaffold delta; habitat-book.borg diff
     additionally strips the agent note; default (no flag) is
     byte-identical to --mode workshop.
   |) *)
   (* agent note (|
     P1.5 (2026-08-30): export + the shared structure pass. The
     borg->LaTeX renderer was refactored onto Book_structure
     (parse + register filter applied ONCE, shared with the new
     book_export.ml JSON projection — habitat-book decision 3).
     Node vocabulary: project/section/subsection/doc/status/
     verify/agent-note/untracked/inline/raw. Comment-drain order
     reproduces print v2 exactly; the loop-4 determinism harness
     (two-run PDF byte-compare) doubles as the refactor regression
     check. Also: Project.build_tree now catches the lexer's plain
     Failure (not just Parse_error) so a parse-broken inlined
     chapter degrades instead of crashing the walk. One deliberate
     print delta: (inline <atom>) targets now render the
     navigation note — pre-v2 print silently dropped atom-form
     inlines (only quoted strings rendered); Spec.inline_targets
     accepts both spellings, so the note must too (pricklypear
     writes atom-style exclusively). Verified:
     alcotest book_export 6 cases (shape/order, mode filter, raw
     fallback, byte-determinism + no-abs-paths, --root subtree,
     atom inline target); full pricklypear export = 34 chapters in
     inline order + 266 code chapters incl. libs/spec/lib.pp and
     all 26 migrations, 33 inline nav nodes, two runs
     byte-identical; print re-verified on the full tree.
   |) *)
   (verify
    (build
    )
    (smoke
     "borge book print"
    )
   )
   (details
    (doc (|
         lib/book/book_print.ml — the linearizer and LaTeX emitter.

         v2 scope: inline-aware ordering (the root project's
         (inline ...) tree is the chapter order), byte-deterministic
         PDFs, --root FILE subtree selection. No woven view, no
         --section/--woven yet. Unreferenced files (.ml/.mli code,
         stray .borg) append lexicographically as the appendix.
         Each file is read, sanitized, emitted as one LaTeX
         document, run through pdflatex.

         Pipeline:
         1. collect_files : dir -> string list
              Roots via Project.find_roots, canonicalized to paths
              relative to dir (canon_path folds . and .. — course
              roots reference chapters as ../borge/x.borg and must
              dedup against the root-written spelling), minus course
              composition roots (course/* — chapter selections for
              --root, never canonical chapters; skip_dirs keeps them
              out of the appendix too). For each root (lexicographic
              when there are several), Project.build_tree follows
              (inline ...) targets and Project.tree_paths flattens
              the subtree depth-first — that flatten IS the chapter
              order (deduped, first occurrence wins). A root whose
              tree fails to build (missing target, cycle, parse
              error) degrades to a lone chapter, warning on stderr.
              Then a deterministic walk of all .borg (excl.
              .borg.meta), .mli, .ml, .pp, .sql files (pp-sync vault
              dumps under backups/ are skipped as gitignored build
              artifacts); whatever the inline tree did not reference
              is appended, sorted lexicographically. No readdir order
              survives. Skip _build/, .git/, and .gitignore build
              artifacts.
              (Obituary: the v1 four-tier + dependency-rank ordering
              was retired for this walk — spec
              borge/habitat-book.borg export-projection requires
              chapter order == inline order, shared by print,
              export, and the habitat loader.)
         1b. collect_subtree : dir -> root_file:string -> string list
              The --root FILE walk: Project.build_tree rooted at
              FILE (resolved relative to dir, then CWD), flattened —
              exactly the subtree, nothing else (no appendix, no
              sibling roots). Missing file or unbuildable tree is a
              hard error: an explicit selection is a contract, not
              an implicit whole-book default.
         2. file_to_latex : path -> lines -> string
              For .ml/.mli/.pp/.sql: \section{path} +
              \lstinputlisting block (numbers=left, frame=single,
              mono, breaklines, wide right margin for marginnote
              annotations), cited under the file's own extension.
              For .borg: Book_structure.of_file — the SHARED pass
              (one parser, two projections: parse + register filter
              applied once, consumed by print AND export) — then
              emit LaTeX from the node tree (headings, prose;
              workshop register only: status italic marker, verify
              compact scaffold line, agent-note blockquote,
              untracked small italic note; inline -> small note in
              BOTH registers; depends-on/convention always
              skipped). Doc prose is segmented by the shared fence
              parser (fence_split): text -> paragraphs, ```nopales/
              ```ocaml example fences -> lstlisting blocks (no line
              numbers) + the [expect => ...] captured-output
              annotation, in BOTH registers. Reader strips the
              workshop-only scaffolding. Falls back to a raw listing
              on parse failure — the print-side drift hole, visible
              in both registers.
         3. render : dir -> stem -> unit
              Assemble preamble + body + \end{document}, shell out
              to pdflatex (loop passes until .aux/.toc stabilize),
              write the manifest sidecar via book-manifest-impl.

         Backend selection at runtime: pdflatex on PATH -> LaTeX;
         else groff on PATH -> groff/ms; else error. Not config.

         Shell-out uses Unix.open_process and checks the exit code;
         a failed pdflatex is a hard error, not a silent fallback.

         Determinism: pdflatex runs with cwd = the work directory
         and cites sanitized listings by RELATIVE path (no temp-dir
         name can leak into the .tex), under SOURCE_DATE_EPOCH +
         FORCE_SOURCE_DATE so the PDF's timestamps and /ID derive
         from a fixed epoch. The PDF is a pure function of the
         printed tree; rendered_at in the manifest is the sole
         wall-clock artifact.
       |))
   )
  )
  (subsection book-export-impl
   (doc "borge book export — the JSON projection of the shared walk (habitat loader feed)")
   (status implemented)
   (details
    (doc (|
         lib/book/book_export.ml (+ lib/book/book_structure.ml, the
         shared parse+register pass consumed by print AND export —
         decision 3: one parser, two projections, no forked
         traversal). Writes <stem>.book.json; shape per
         borge/habitat-book.borg export-projection (the pricklypear
         loader is the client — keep in lockstep):

         {root, mode, chapters:[{slug, title, status, nodes,
          examples}], code-chapters:[{path, lang, source}]}

         - chapters: .borg files in walk order (Book_print walk,
         same collect_files / collect_subtree as print). Node
         kinds: project/section/subsection headings, doc, and the
         register-filtered scaffolding; examples = the fenced
         example records (P1.6): one {lang, src, expected?} per
         ```nopales/```ocaml doc fence, document order, both
         registers; expected carries the `expect =>` trailer or
         null. Same fence parser print renders from (decision 3);
         unparsed fences stay raw text in the doc body.
         - code-chapters: true source bytes (sanitize is a
         pdflatex concern only).
         - Determinism: no wall clock, no absolute paths — two
         runs byte-identical, loader checksums against this.
         - Drift hole: unparseable chapter -> one raw node with
         the true source, both registers.
       |))
   )
  )
  (subsection book-manifest-impl
   (doc "Manifest sidecar: page N -> file, plus a tree checksum")
   (status implemented)
   (* agent note (|
     verified at this commit: build-pass. start_page per file parsed
     from the .aux (\newlabel{file:...}), exact — no estimation.
     Line numbers are NOT stored (printed on the page by listings).
   |) *)
   (details
    (doc (|
         lib/book/manifest.ml — the round-trip anchor. JSON (yojson).

         Fields per book-manifest in borge.borg: stem, rendered_at,
         backend, tree_checksum, pages. tree_checksum is a sha1 over
         the ordered (path, content-sha1) list — ordered so a file
         reorder is detectable, not just content changes.

         Line numbers are printed on the page by listings
         (numbers=left) and read off the page by absorb — the
         manifest never stores line ranges. pages entries carry
         only n (page number) and file (source path on that page),
         so a note in the gutter between two files is unambiguous.
         This avoids parsing PDF page breaks entirely: the line
         precision comes free from the rendered page, not from a
         sidecar.

         Functions:
         - build : dir:string -> files:string list -> manifest
         - to_json : manifest -> string
         - tree_checksum : files:string list -> string
       |))
   )
  )
  (subsection book-absorb-impl
   (doc "Scan -> inbox items. NOT implemented in v1.")
   (status planned)
   (details
    (doc (|
         lib/book/absorb.ml — stub in v1. The full pipeline (vision
         LLM per page, manifest lookup, inbox emission) is spec'd
         in borge.borg book-absorb but is out of scope for the first
         print pass. This subsection exists so the module has a home
         when it lands.
       |))
   )
  )
 )
)
